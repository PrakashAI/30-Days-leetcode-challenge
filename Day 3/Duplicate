#include<stdio.h>

int main()
{int n,i,j,temp;
   printf ("Enter the size of array");
   scanf("%d",&n);
   int a[n];
   for(i=0;i<n;i++){
   scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++){
    for(j=i+1;j<n;j++){
        if(a[i]>a[j]){
         temp=a[i];
         a[i]=a[j];
         a[j]=temp;
     }
     } 
       }
       
    for(i=0;i<n;i++){
       if(a[i]==a[i+1]){
           printf("Duplicate is %d\n",a[i]); 
            break;}   

}}
